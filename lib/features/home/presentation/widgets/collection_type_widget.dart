import 'package:flutter/material.dart';

class CollectionTypeWidget extends StatelessWidget {
  const CollectionTypeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}